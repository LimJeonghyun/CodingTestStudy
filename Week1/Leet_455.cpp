#include <iostream>
#include <vector>
using namespace std;

//children 인원과 cookies 갯수 비교

class solution{
    public:
    int findContentChildren(vector<int>& g, vector<int>& s){
        bool check;
        int mresult;

        for(int i =0; i<g.size();i++){
            mresult = s.find(g[i]);
            if (mresult != -1) 
        }
    }
};

int main(){
    return 0;
}